- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.shell:
    ~/.dotfiles: ''
    ~/.vim: 
    ~/.vimrc: 
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
